kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: hosts-configmap-reader
  namespace: openshift-dns
rules:
- apiGroups:
  - ""
  resources:
  - configmaps
  resourceNames:
  - hosts-file
  verbs:
  - get
  - list
  - watch
