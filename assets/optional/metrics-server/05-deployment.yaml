apiVersion: apps/v1
kind: Deployment
metadata:
  name: metrics-server
  namespace: openshift-monitoring
  labels:
    app.kubernetes.io/name: metrics-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: metrics-server
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: metrics-server
      annotations:
        target.workload.openshift.io/management: '{"effect": "PreferredDuringScheduling"}'
        openshift.io/required-scc: restricted-v2
    spec:
      serviceAccountName: metrics-server
      priorityClassName: system-cluster-critical
      containers:
        - name: metrics-server
          image: quay.io/openshift/metrics-server:latest
          imagePullPolicy: IfNotPresent
          args:
            - --secure-port=4443
            - --kubelet-preferred-address-types=InternalIP,Hostname
            - --kubelet-use-node-status-port
            - --metric-resolution=15s
            - --tls-cert-file=/etc/tls/serving/tls.crt
            - --tls-private-key-file=/etc/tls/serving/tls.key
            - --kubelet-client-certificate=/etc/tls/kubelet-client/tls.crt
            - --kubelet-client-key=/etc/tls/kubelet-client/tls.key
            - --kubelet-certificate-authority=/etc/tls/kubelet-ca/ca-bundle.crt
          ports:
            - containerPort: 4443
              name: https
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /readyz
              port: https
              scheme: HTTPS
            periodSeconds: 10
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /livez
              port: https
              scheme: HTTPS
            periodSeconds: 10
            failureThreshold: 3
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
          volumeMounts:
            - name: serving-cert
              mountPath: /etc/tls/serving
              readOnly: true
            - name: kubelet-client-cert
              mountPath: /etc/tls/kubelet-client
              readOnly: true
            - name: kubelet-ca
              mountPath: /etc/tls/kubelet-ca
              readOnly: true
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: serving-cert
          secret:
            secretName: metrics-server-tls
        - name: kubelet-client-cert
          secret:
            secretName: metrics-server-kubelet-client
        - name: kubelet-ca
          configMap:
            name: metrics-server-kubelet-ca
            items:
              - key: ca-bundle.crt
                path: ca-bundle.crt
        - name: tmp
          emptyDir: {}
      nodeSelector:
        kubernetes.io/os: linux
        node-role.kubernetes.io/master: ""
      tolerations:
        - key: node-role.kubernetes.io/master
          operator: Exists
