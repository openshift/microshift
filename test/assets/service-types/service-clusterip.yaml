apiVersion: v1
kind: Service
metadata:
  name: hello-deploy
  labels:
    app: hello-deploy
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
  selector:
    app: hello-deploy
