---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: ${CERT_NAME}
  namespace: ${NAMESPACE}
spec:
  secretName: ${SECRET_NAME}
  issuerRef:
    name: ${ISSUER_NAME}
    kind: ClusterIssuer
  commonName: ${CERT_COMMON_NAME}
  dnsNames:
  - ${CERT_DNS_NAME}
  - www.${CERT_DNS_NAME}
