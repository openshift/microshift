apiVersion: v1
kind: Pod
metadata:
  name: ${PODNAME}
spec:
  securityContext:
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault
  containers:
  - name: ${PODNAME}
    image: ${IMAGE}
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop:
        - ALL
    volumeMounts:
    - name: data
      mountPath: ${MOUNTPATH}
  volumes:
  - name: data
    persistentVolumeClaim:
      claimName: ${PVCNAME}
