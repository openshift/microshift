{{- if .Previous.Repository -}}
id = "microshift-prel"
name = "Repository with RPMs for previous release"
type = "yum-baseurl"
url = "{{ .Previous.Repository }}"
check_gpg = false
check_ssl = true
system = false
{{- end -}}
